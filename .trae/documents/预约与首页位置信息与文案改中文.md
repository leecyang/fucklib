## 改动概览

* 预约页下拉框占位文案改为中文（“选择图书馆与楼层...”）。

* 预约页“当前有效预约”的《位置》改为显示“完整馆名（xx阅览区）+ 楼层（x楼）”。

* 首页用户模块明确展示“学号：xxxx”，保留学校信息展示。

* 首页“当前座位”模块的《位置》改为显示“完整馆名 + 楼层”。

## 具体修改

* 预约页占位文案

  * 修改位置：`frontend/src/pages/InteractiveReserve.tsx:304`

  * 现状：`<option value="">Select Library & Floor...</option>`

  * 修改为：`<option value="">选择图书馆与楼层...</option>`（或统一术语为“选择场馆与楼层...”）

* 预约页“当前有效预约”位置展示

  * 位置：`frontend/src/pages/InteractiveReserve.tsx:331–353`

  * 逻辑：从 `libs` 找到当前 `lib`，`lib.name` 由后端拼接为 `lib_name - lib_floor`。

  * 修改：

    * 计算 `libName = lib ? (lib.name.split(' - ')[0] || lib.name) : (reserveInfo.lib_name || '')`

    * 计算 `floor = lib ? (lib.name.split(' - ')[1] || lib.name) : (reserveInfo.lib_floor || libId)`

    * 渲染“位置：{libName}（{floor}）”。

  * 代码参考当前仅显示楼层：`frontend/src/pages/InteractiveReserve.tsx:351`

* 首页用户模块展示学号

  * 位置：`frontend/src/pages/Dashboard.tsx:92–95`

  * 现状：姓名行下直接显示学号数值，占位为 `No ID`。

  * 修改：改为带中文标签的展示，如：`学号：{userInfo?.user_student_no || '-'}`，保留学校信息行：`frontend/src/pages/Dashboard.tsx:98–104`。

* 首页“当前座位”位置展示

  * 位置：`frontend/src/pages/Dashboard.tsx:133–147`

  * 逻辑：当前仅显示“楼层：{floor}`，从 ` lib.name.split(' - ')\[1]\` 获取。

  * 修改：

    * 计算 `libName = lib ? (lib.name.split(' - ')[0] || lib.name) : (seat.lib_name || '')`

    * 计算 `floor = lib ? (lib.name.split(' - ')[1] || lib.name) : (seat.lib_floor || seat.lib_id)`

    * 徽标或文案改为“位置：{libName}（{floor}）”。

  * 代码参考当前徽标：`frontend/src/pages/Dashboard.tsx:146–147`

## 回退与兼容

* 若 `libs` 未能获取到，位置显示回退为后端 `reserve` 提供的 `lib_name/lib_floor` 字段；若仍为空，则保守回退为 `lib_id` 以避免空白。

* 保持现有状态文案与其他 UI 不变，确保改动只影响文案与位置组合展示。

<br />

