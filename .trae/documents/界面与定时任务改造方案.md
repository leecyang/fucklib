## 个人信息模块
- 移除显示字段 `性别` 与 `上次登录`：`frontend/src/pages/Dashboard.tsx:69–75`

## 状态概览
- 在“状态概览”卡片右上添加按钮 `去设置`，跳转到 `Settings` 页：`/settings`

## 定时任务概览（前端）
- 将表格列标题改为中文：`任务类型`、`Cron`、`启用`、`上次运行时间`、`上次状态`、`信息`
- 从类型下拉中移除“今日预约/明日预约”，仅保留两项：`签到`（signin）与 `预约`（reserve）：`frontend/src/pages/ScheduledTasks.tsx`

## 创建定时任务：交互式座位选择
- 新增座位选择弹窗（SVG 交互）：复用交互页的座位渲染逻辑，抽成组件 `SeatPicker`
  - 数据源：`libApi.getLayout(libId)`（返回 `seats: [{x,y,key,name,status}]`）
  - 交互：点击可预约的座位（`status === 1`）即选中，返回 `seat_key`
- 表单改造：
  - 不再输入坐标（`x/y`）或直接输入 `seat_key`
  - 先选择 `图书馆`（与楼层），点击“选择座位”打开 `SeatPicker`，完成后将选中的 `seat_key` 写入任务配置
- 改动位置：`frontend/src/pages/ScheduledTasks.tsx`（任务创建/编辑表单）；座位弹窗组件新增 `frontend/src/components/SeatPicker.tsx`

## 任务类型与后端映射
- 统一任务类型仅两种：`signin` 与 `reserve`
- 移除 `seat_today`、`seat_tomorrow` 的调度与创建支持：`backend/app/scheduler.py`、`backend/app/routers/tasks.py`
- `reserve` 映射到座位预约：`LibService.reserve_seat(lib_id, seat_key)`：`backend/app/services/lib_service.py:147–173`
- 现有历史任务迁移策略：将 `seat_today` 转换为 `reserve`；`seat_tomorrow` 设为禁用或提示删除

## 常用座位展示
- 仅展示三项：`楼层`、`座位`、`状态`；不展示 `座位键` 与 `图书馆ID`
- `楼层`：依据 `lib_id` 映射 `lib_floor`（来源 `GET /library/list`）：`backend/app/services/lib_service.py:218–233`
- `座位` 名称：通过 `GET /library/{lib_id}/layout` 按 `seat_key` 找到 `name`：`backend/app/services/lib_service.py:242–256`
- `状态`：使用布局中座位的 `status/seat_status` 判断，显示 `可预约/不可预约`
- 改动位置：`frontend/src/pages/Dashboard.tsx:102+` 与 `frontend/src/pages/InteractiveReserve.tsx:188+`

## 预约页展示规范
- 当前已有预约的信息展示不再显示：`座位坐标`、`图书馆ID`、`状态码`
- 改为展示：`楼层`（由 `lib_id` 映射 `lib_floor`）、`座位号/名称`（由 `seat_key` 映射布局 `name`）、`状态`（中文文案，如 `已入座/未签到/可预约/不可预约`）
- 改动位置：预约信息卡片 `frontend/src/pages/InteractiveReserve.tsx:143+`

## 快捷预约可用状态
- 在“常用座位快捷预约”区块显示每条座位的当前可预约状态徽标，依据布局 `seat.status === 1`：`frontend/src/pages/InteractiveReserve.tsx:101–124`

## 验证
- Dashboard：个人信息不再出现 `性别/上次登录`；状态概览显示 `去设置`；定时任务卡列为中文；常用座位仅楼层/座位/状态
- ScheduledTasks：创建任务时弹出座位 SVG 选择，不再输入坐标或键；类型仅 `签到/预约`
- 预约页：标题为“预约”，当前预约卡片展示楼层/座位/状态，无 `libId/seatKey/状态码` 原始字段